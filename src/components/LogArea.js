import React from "react";

let instance;
let log = "";

class LogArea {
  // return <div style={{ height: "111px" }}>{props.logValue}</div>;
}

const Logger = Object.freeze(new LogArea());
export default Logger;
